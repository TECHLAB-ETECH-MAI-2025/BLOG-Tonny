{% extends 'base.html.twig' %}

{% block title %}Page non trouvée{% endblock %}

{% block body %}
    <div class="min-h-screen bg-gradient-to-br from-blue-50 to-blue-100 flex items-center justify-center px-4">
        <div class="max-w-lg w-full bg-white rounded-xl shadow-lg overflow-hidden">
            <div class="bg-blue-600 px-6 py-4">
                <div class="flex items-center">
                    <svg class="h-8 w-8 text-white" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.172 16.172a4 4 0 015.656 0M9 10h.01M15 10h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
                    </svg>
                    <h1 class="ml-3 text-xl font-semibold text-white">{{ status_code }} | {{ status_text }}</h1>
                </div>
            </div>

            <div class="px-6 py-8">
                <div class="text-center">
                    <h2 class="text-2xl font-bold text-gray-800 mb-4">Oups ! Cette page est introuvable</h2>
                    <p class="text-gray-600 mb-6">La page que vous recherchez n'existe pas ou a été déplacée.</p>

                    <div class="space-y-4">
                        <a href="{{ path('app_home') }}" class="inline-block bg-blue-600 hover:bg-blue-700 text-white font-medium px-6 py-3 rounded-lg transition duration-200">
                            Retour à l'accueil
                        </a>

                        <div class="text-gray-500 mt-6">
                            <p class="text-sm">Vous pouvez également :</p>
                            <ul class="text-sm mt-2 space-y-1">
                                <li>• Vérifier l'URL pour des erreurs de frappe</li>
                                <li>• Utiliser la recherche sur notre site</li>
                                <li>• Revenir à la page précédente</li>
                            </ul>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
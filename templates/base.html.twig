<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{% block title %}Blog Symfony{% endblock %}</title>
    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
    {# Fontawesome CSS #}
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    <script src="https://cdn.tailwindcss.com"></script>
    {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
    {% endblock %}

    {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
    {% endblock %}
</head>
<body class="flex flex-col min-h-screen">
<nav class="bg-gray-800 text-white p-4">
    <div class="container mx-auto flex justify-between items-center">
        <a class="text-xl font-bold" href="{{ path('app_home') }}">Blog</a>
        <button class="block md:hidden">
            <svg class="fill-current h-6 w-6" viewBox="0 0 24 24">
                <path d="M4 5h16a1 1 0 0 1 0 2H4a1 1 0 1 1 0-2zm0 6h16a1 1 0 0 1 0 2H4a1 1 0 0 1 0-2zm0 6h16a1 1 0 0 1 0 2H4a1 1 0 0 1 0-2z"></path>
            </svg>
        </button>
        <div class="hidden md:flex md:items-center">
            <ul class="flex">
                <li class="mr-6">
                    <a class="text-white" href="{{ path('app_home') }}">Accueil</a>
                </li>
            {% if is_granted('ROLE_ADMIN') %}
                <li class="mr-6">
                    <a class="text-white" href="{{ path('app_article_index') }}">Articles</a>
                </li>
                <li class="mr-6">
                    <a class="text-white" href="{{ path('app_category_index') }}">Catégories</a>
                </li>
            {% endif %}
            </ul>
            <ul class="flex">
                {% if app.user %}
                    <li class="mr-6">
                        <span class="text-white">Bonjour, {{ app.user.username }}</span>
                    </li>
                    <li class="mr-6">
                        <a class="text-white" href="{{ path('app_logout') }}">Se déconnecter</a>
                    </li>
                {% else %}
                    <li class="mr-6">
                        <a class="text-white" href="{{ path('app_login') }}">Se connecter</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>

<div class="container mx-auto p-4">
    {% for label, messages in app.flashes %}
        {% for message in messages %}
            <div class="bg-{{ label }}-100 border border-{{ label }}-400 text-{{ label }}-700 px-4 py-3 rounded relative" role="alert">
                {{ message }}
                <span class="absolute top-0 bottom-0 right-0 px-4 py-3">
                    <svg class="fill-current h-6 w-6 text-{{ label }}-500" role="button" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20"><title>Close</title><path d="M14.348 14.849a1.2 1.2 0 0 1-1.697 0L10 11.819l-2.651 3.029a1.2 1.2 0 1 1-1.697-1.697l2.758-3.15-2.759-3.152a1.2 1.2 0 1 1 1.697-1.697L10 8.183l2.651-3.031a1.2 1.2 0 1 1 1.697 1.697l-2.758 3.152 2.758 3.15a1.2 1.2 0 0 1 0 1.698z"/></svg>
                </span>
            </div>
        {% endfor %}
    {% endfor %}

    {% block body %}{% endblock %}
</div>

<footer class="bg-gray-800 text-white mt-auto p-4">
    <div class="container mx-auto">
        <div class="flex flex-col md:flex-row justify-between">
            <div>
                <p>&copy; {{ "now"|date("Y") }} - Blog Symfony</p>
            </div>
        </div>
    </div>
</footer>

</body>
</html>
